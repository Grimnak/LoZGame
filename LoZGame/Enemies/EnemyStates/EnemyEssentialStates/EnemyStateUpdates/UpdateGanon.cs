namespace LoZClone
{
    using Microsoft.Xna.Framework;
    using Microsoft.Xna.Framework.Graphics;

    public partial class EnemyStateEssentials
    {
        public void UpdateGanon()
        {
            /*Lifetime++;
            if (((Ganon)Enemy).VisibilityTimer > 0 || Enemy.Health.CurrentHealth <= 4)
            {
                Sprite.Update();
            }
            else
            {
                Sprite = null;
            }
            if (Lifetime > DirectionChange && !Enemy.Physics.IsJumping)
            {
                Enemy.UpdateState();
                Lifetime = 0;
            }*/
        }
    }
}